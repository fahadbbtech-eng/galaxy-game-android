// Top-level build file. This is where Android Studio configures shared plugin versions
// for every module in the project (we only have one module: "app").
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
